package com.mediacenter.app.data.model

enum class MediaType {
    FOLDER,
    IMAGE,
    VIDEO,
    WEB,
    TEXT,
    FILE,
}

enum class MediaFilter {
    ALL,
    IMAGE,
    VIDEO,
    WEB,
    TEXT,
}

enum class SortMode {
    NAME,
    DATE,
    TYPE,
}
